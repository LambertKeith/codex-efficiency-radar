# Codex Efficiency Radar 项目规则

## 完整安装契约

- 原生模型选择器增强是本项目的必需组成，不是可选附加项。
- 只有正规 Codex 插件已启用、当前客户端命中精确兼容白名单、平台启动项正确、Codex 已自动进入增强模式，且端到端检查在真实模型选择器中确认“效率”入口与匹配当前快照的数值时，安装器才可返回成功。
- `Install.sh` 与 `Install.cmd` 是唯一受支持的完整安装入口。裸 `codex plugin add` 不会安装系统级运行时，不能被描述为完整安装。
- 不提供 `plugin-only` 成功路径。Windows、macOS 以外的平台必须在修改 marketplace 或插件状态前明确失败。
- 未审核的 Codex 构建必须失败；禁止绕过版本、签名、架构或 `app.asar` 哈希校验。
- 新增兼容目标前必须核验选择器契约，并为完整身份匹配与安装健康状态补充测试。

## 验证

- Node.js 项目统一使用 pnpm。
- 修改安装器、运行时或兼容目标后必须运行 `pnpm check`。
- 不得以“进程曾出现”、`armed`、等待重启或调试端口存在代替完成检查；安装器应核对当前安装尝试 ID、Resident 版本、平台、兼容目标、真实 UI 证据、数值快照一致性与熔断状态。
